enum Warna {
  Merah,
  Biru,
  Hijau,
}

let warnaPilihan: Warna = Warna.Biru;

console.log(warnaPilihan);

enum Status {
  Aktif = 'ACTIVE',
  NonAktif = 'INACTIVE',
}

let statusPengguna: Status = Status.Aktif;

console.log(statusPengguna);